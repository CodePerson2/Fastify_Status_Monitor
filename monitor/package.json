{
  "name": "monitor",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "rabbitmq:up": "docker-compose -f docker-compose.integration.yml up -d",
    "rabbitmq:down": "docker-compose -f docker-compose.integration.yml down",
    "test:integration": "npm run rabbitmq:up && vitest run integration && npm run rabbitmq:down"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "abort-controller": "^3.0.0",
    "amqplib": "^0.10.7",
    "fastify": "^5.3.1"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  }
}
